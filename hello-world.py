# Without modification, hello-world.py will produce an error when run.
# Find the cause of error and fix it. 

name = input("Enter name: ")
print(f"Hello, world!")
print(f"Checking whether your name equals ADMIN: {name = 'ADMIN'}")
